services:
  app:
    build:
      context: .
      args:
        DOWNLOAD_WEIGHTS: "true"
    ports:
      - "8000:8000"
    environment:
      HF_HOME: /app/models
      FRONTEND_DIST: /app/frontend_dist
